{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Terminal",
            "type": "python",
            "request": "launch",
            "pythonPath": "${workspaceRoot}/.venv/bin/python",
            "module": "mycloud",
            "stopOnEntry": true,
            "console": "integratedTerminal",
            "args": [
                "${input:args}"
            ],
            "cwd": "${workspaceFolder}"
        }
    ],
    "inputs": [
        {
            "id": "args",
            "type": "command",
            "command": "Command line args for debugging"
        }
    ]
}